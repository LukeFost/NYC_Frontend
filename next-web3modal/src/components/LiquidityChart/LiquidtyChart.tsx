interface iLiquidityChart {
  poolAddress: `0x${string}`;
  minBounds: number;
  maxBounds: number;
}
const LiquidityChart = ({
  poolAddress,
  minBounds,
  maxBounds,
}: iLiquidityChart) => {
  return <></>;
};

export default LiquidityChart;
